/* eslint-disable @typescript-eslint/naming-convention */
"use strict";

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up() {
    // Migration no longer needed because rating stays as INTEGER.
  },

  async down() {},
};
